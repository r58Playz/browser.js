{
	"entries": [
		{
			"key": "T1|value-divergence|URL.createObjectURL|other",
			"cause": "A blob UUID cannot be made to match across the two sides, because making it match would break the browser. Blob URLs are registered in the BROWSER process and shared across renderers, so two renderers minting the same UUID is a duplicate registration and the second renderer is killed outright (\"Invalid UUID passed to BlobRegistry::Register\") -- which on this very site killed the Cloudflare iframe. 07-determinism.patch therefore puts kSbxdiffStreamBlobUuid in the process-DISTINCT set and mixes process identity into its keystream. The oracle and the sandbox have different process topologies by construction: site isolation gives the oracle a renderer per site, while the sandbox serves every origin from localhost:4500 in one. Different process identity, different keystream, different UUIDs. This is FINDINGS #83 exactly -- a fix keyed on process topology is asymmetric between the two sides by construction.",
			"falsifiedBy": "A blob UUID stream anchored to a REALM rather than a process, that still guarantees cross-renderer uniqueness. Or evidence that Cloudflare reads the UUID itself rather than merely holding the URL -- the values are random per run for a real visitor too, so a server cannot expect any particular one."
		},
		{
			"key": "T1|value-divergence|MemoryInfo.totalJSHeapSize.get|numeric-delta",
			"cause": "performance.memory reports the V8 isolate's heap, and the sandbox's isolate legitimately holds more than the oracle's: the guest page, the Turnstile widget and scramjet itself all share one, where site isolation gives the oracle a separate renderer -- and therefore a separate isolate -- for the widget. window_performance.cc:370 returns PRECISE, unbucketized values when the renderer is site-locked, which both sides are, so there is not even a quantisation step to hide inside. dom/memory.ts already subtracts the shim's footprint measured at init; the residual is allocation scramjet makes AFTER that, rewriting the guest's own scripts, and it is proportional to the guest's content rather than a constant that could be subtracted.",
			"falsifiedBy": "Cloudflare branching on the heap value rather than merely collecting it, or a live run that reaches the page with the heap still divergent -- which would show it is collected and not graded. Also falsified if the residual grows: the bound below is what it measured when this was written.",
			"maxSpread": 20000000
		},
		{
			"key": "T1|value-divergence|MemoryInfo.usedJSHeapSize.get|numeric-delta",
			"cause": "The same isolate, for the same reason as totalJSHeapSize above: the sandbox's V8 isolate holds the page, the widget and scramjet where the oracle's holds only the widget, and site-locked renderers report precise rather than bucketized values. Subtracting a constant cannot work because the shim's post-init allocation scales with how much guest source it has had to rewrite.",
			"falsifiedBy": "The same as totalJSHeapSize: Cloudflare branching on the value, or a live pass with it still divergent. Or a scramjet that rewrites without retaining, which would make the residual a constant and therefore subtractable.",
			"maxSpread": 25000000
		},
		{
			"key": "T1|value-divergence|HTMLCollection.length.get|numeric-delta",
			"cause": "This is read in the MAIN page's realm (https://rateyourmusic.com/), not the interstitial's -- a realm the sandbox only ever reaches under replay, where the store answers the challenge POST with its recorded 200 whatever was sent (RULES #85). So it describes a journey the live sandbox does not complete, and it cannot be the reason live fails. document.scripts is LIVE, so its length is not a property of the document but of where the HTML parser had got to at the instant it was read -- and the reader is googletagmanager's async script, whose arrival time decides that. FINDINGS #100 measured it by prepending a probe to the recorded gtag body, so both sides sampled the same instant of the same code: the oracle reads 20 because the parser is blocked on the <script src> at markup index 19 and gtag runs in that gap, and the sandbox reads 23 because gtag's 343 KB body has to go through the JS rewriter first, arrives later, and by then the parser has reached EOF. The three extra elements are exactly markup indices 20, 21 and 22, the document's last three script tags. Nothing in scramjet chooses this and nothing in scramjet can pin it: it is the relative latency of one async subresource against another server's, and a real browser on a real network would not reproduce the oracle's 20 either, because googletagmanager.com and cdn.sonemic.net are different hosts with different latencies.",
			"falsifiedBy": "A divergence in this bucket in the INTERSTITIAL or widget realm rather than the main page's, which would mean the challenge itself reads it. A divergence in this bucket that is not document.scripts, or one whose spread is not the three trailing script tags -- both of which the magnitude bound below will catch. Also falsified if Cloudflare is shown to branch on the count rather than merely hash it, or if rateyourmusic breaks live at this read.",
			"maxSpread": 3
		}
	]
}
